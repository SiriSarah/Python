# Tuples 

my_tuple = (1, 2, 3, 4, 5)

print(my_tuple)

# Tuples can't be modified, so we can assign new values but not modify the existing ones.

new_tuple = (6, 7)

my_new_tuple = my_tuple + new_tuple

print(my_new_tuple)